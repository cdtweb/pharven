<?php
\Phar::mapPhar();
{% if mounts %}
{% for mount in mounts %}
\Phar::mount('{{ mount }}', __DIR__ . DIRECTORY_SEPARATOR . '{{ mount }}');
{% endfor %}
{% endif %}
return require "phar://pharven.phar/autoload.php";
__HALT_COMPILER();